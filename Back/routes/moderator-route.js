const express = require('express');
const router = express.Router();
const modCtrl = require('../controllers/moderator-controller');

// router.post('/moderator',  modCtrl.createPost);

module.exports = router;